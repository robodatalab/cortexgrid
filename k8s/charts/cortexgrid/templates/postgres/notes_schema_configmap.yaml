{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: notes-schema
  labels:
    {{- include "cortexgrid.labels" . | nindent 4 }}
data:
  notes_schema.sql: |-
{{ .Files.Get "files/postgres/notes_schema.sql" | indent 4 }}
{{- end }}
