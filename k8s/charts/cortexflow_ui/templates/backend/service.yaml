apiVersion: v1
kind: Service
metadata:
  name: cortexflow-ui-backend
  labels:
    {{- include "cortexflow_ui.labels" . | nindent 4 }}
    app: cortexflow-ui-backend
spec:
  type: ClusterIP
  selector:
    app: cortexflow-ui-backend
  ports:
    - name: http
      port: {{ .Values.services.backend.port }}
      targetPort: {{ .Values.services.backend.port }}
