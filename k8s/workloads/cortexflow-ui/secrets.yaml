apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cortexflow-ui-public-urls
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: cortexflow-ui-public-urls
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    # Explicit defaults included so Argo doesn't see drift against ESO's
    # CRD-defaulting webhook (which fills these in on the live object).
    - secretKey: PUBLIC_MLFLOW_URL
      remoteRef:
        key: robolab/infra/MLFLOW_TRACKING_URI
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
    - secretKey: PUBLIC_RAY_DASHBOARD_URL
      remoteRef:
        key: robolab/infra/RAY_JOB_SERVER_URI
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
