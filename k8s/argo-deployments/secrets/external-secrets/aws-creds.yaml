apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: aws-creds
  namespace: external-secrets
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: aws-creds
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        annotations:
          reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
          reflector.v1.k8s.emberstack.com/reflection-auto-enabled: "true"
  data:
    - secretKey: AWS_ACCESS_KEY_ID
      remoteRef:
        key: robolab/infra/AWS_ACCESS_KEY_ID
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
    - secretKey: AWS_SECRET_ACCESS_KEY
      remoteRef:
        key: robolab/infra/AWS_SECRET_ACCESS_KEY
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
    # S3_BUCKET_NAME piggybacks on aws-creds so any pod with `envFrom: aws-creds`
    # gets it for free (mlflow, cortexflow-ui-backend, jobs-control-plane, ray).
    - secretKey: S3_BUCKET_NAME
      remoteRef:
        key: robolab/infra/S3_BUCKET_NAME
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
