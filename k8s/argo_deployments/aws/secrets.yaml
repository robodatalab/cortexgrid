apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: secrets
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/robodatalab/robolab-infra.git
    targetRevision: main
    path: k8s/argo_deployments/aws/secrets
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    # Wave -1 (external-secrets, reflector) Applications are created, then
    # wave 0 (ClusterSecretStore) tries to apply before the external-secrets
    # webhook pod is Ready. Tight retry recovers in ~30-60s vs Argo's default
    # 3+ min.
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 2m
