[project]
name = "cortexgrid-example-jobs"
version = "0.1.0"
description = "Example: submit remote jobs to the DGX via cortexgrid"
requires-python = ">=3.11"
dependencies = [
    "cortexgrid",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
