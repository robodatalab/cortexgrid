name: ray-head

on:
  push:
    paths:
      - "k8s/docker/ray/**"
      - ".github/workflows/ray-head.yml"

permissions:
  contents: write
  packages: write

jobs:
  build-and-push:
    runs-on: arc-runners

    env:
      IMAGE: ghcr.io/${{ github.repository_owner }}/ray-head

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute branch slug
        id: slug
        run: echo "value=$(echo '${{ github.ref_name }}' | tr '/' '-')" >> "$GITHUB_OUTPUT"

      - name: Build and push
        run: |
          docker buildx build --platform linux/arm64,linux/amd64 \
            -f k8s/docker/ray/Dockerfile \
            -t $IMAGE:${{ steps.slug.outputs.value }}-${{ github.sha }} \
            --push .

      - name: Bump image tag in git
        if: github.ref == 'refs/heads/main'
        env:
          NEW_TAG: ${{ steps.slug.outputs.value }}-${{ github.sha }}
        working-directory: k8s/workloads/ray
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          for i in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            NEW_IMAGE="$IMAGE:$NEW_TAG" yq -i '.spec.template.spec.containers[0].image = strenv(NEW_IMAGE)' deployment.yaml
            git add deployment.yaml
            git diff --cached --quiet && exit 0
            git commit -m "bump ray-head to $NEW_TAG"
            git push origin HEAD:main && exit 0
            sleep 3
          done
          exit 1
