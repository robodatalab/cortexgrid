# =============================================================================
# Local Development Overrides
# =============================================================================
# This file is automatically loaded by Docker Compose alongside
# docker-compose.yml. Use it for local dev tweaks that shouldn't be committed.
#
# Examples:
#   - Expose additional debug ports
#   - Mount local code into containers for live reload
#   - Override resource limits for smaller machines
#
# To disable: rename to docker-compose.override.yml.disabled
# =============================================================================

version: "3.8"

services: {}
